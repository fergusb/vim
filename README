# Fergus' vim setup

## Installation
cd [or] cd ~/
git clone git://github.com/fergusb/vim.git .vim
ln -s .vim/vimrc ~/.vimrc

### Init plugins
cd ~/.vim/
git submodule init
git submodule update

Note: for pyflakes to work correctly, use:
  git submodule update --init --recursive

### Add plugins
cd ~/.vim/
mkdir bundle
git submodule add https://github.com/fergusb/vim-foo.git bundle/foo

### Update plugins
git submodule foreach git pull origin master
git submodule update

### YouCompleteMe
To complete the YCM installation
  cd ~/.vim/bundle/YouCompleteMe
  ./install.sh --clang-completer
Full: http://valloric.github.io/YouCompleteMe/

" vim:ft=mkd
